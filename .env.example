# Database Configuration
DB_HOST=localhost
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_NAME=digitalgames_db
DB_PORT=5432

# Para desarrollo local con PostgreSQL, usa estos valores
# Para MySQL local (Laragon), usa DB_PORT=3306

# En Render, estas variables se configuran automáticamente desde el Dashboard
# DATABASE_URL se configura automáticamente cuando conectas una PostgreSQL database

# Application
NODE_ENV=development
PORT=4040

# Session Secret (cambia esto en producción)
SESSION_SECRET=Shhh, It's a secret
